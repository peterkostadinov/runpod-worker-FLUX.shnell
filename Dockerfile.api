FROM python:3.11-slim

ENV DEBIAN_FRONTEND=noninteractive

RUN pip install --no-cache-dir \
    fastapi>=0.115.0 \
    "uvicorn[standard]>=0.32.0" \
    "redis>=4.6.0" \
    python-dotenv>=1.0.0

COPY local_server.py /local_server.py

CMD ["python3", "-u", "/local_server.py"]
